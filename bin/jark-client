#!/usr/bin/env python

import socket, sys

c = socket.create_connection(("localhost", 9000));
f = c.makefile('rw')

f.writelines('(println (jark.%s/%s))\n' % (sys.argv[1], sys.argv[2]))
f.flush()

for line in f:
    if line.strip() == 'nil':
      break
    print line,

