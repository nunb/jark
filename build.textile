---
layout: clojure
head: Jark
---

h2. Building jark

To build jark, you need "lein":https://github.com/technomancy/leiningen to be installed.
The build script also requires javac (part of JDK) to be in PATH.
<pre class="terminal"><code>$ git clone git://github.com/icylisper/jark.git
$ cd jark && lein jar
$ ./build [-server | --client ]
$ ./jark self install
$ cp jark /to/PATH  (where PATH is for example: ~/bin/)
</code></pre>
